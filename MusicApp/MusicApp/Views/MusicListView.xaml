<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MusicApp.Views.MusicListView"
             Title=""
              xmlns:viewModels="clr-namespace:MusicApp.ViewModels"
 xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:components="clr-namespace:MusicApp.Components"
             Shell.NavBarIsVisible="{OnPlatform Android=True, WinUI=false}"
 x:DataType="viewModels:MusicListViewModel"
             x:Name="this">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior Command="{Binding AppearingCommand}" EventName="Appearing" BindingContext="{Binding Source={x:Reference this}, Path=BindingContext}"/>
        <toolkit:EventToCommandBehavior Command="{Binding DisappearingCommand}" EventName="Disappearing" BindingContext="{Binding Source={x:Reference this}, Path=BindingContext}"/>
       
    </ContentPage.Behaviors>

    <Grid Margin="0, 10, 0, 0" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <components:MusicListPlusMediaElementComponent Musics="{Binding Musics}" Grid.Row="0"/>
        <Label Text="{Binding ErrorMessage}" IsVisible="{Binding IsErrorVisible}" FontSize="Title" VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>

</ContentPage>